.index__main
  .index__main--timeline__tweetbox
    .index__main--timeline__tweetbox__area
      - if @path.is('tweets#index')
        %i.fa.fa-home.awesome ホーム
  .index__main--stream__box
    = render "layouts/mainTweet"
  = paginate(@tweets)
  .page-load-status
    .infinite-scroll-request.loader-wheel
      %i
        %i
          %i
            %i
              %i
                %i
                  %i
                    %i
                      %i
                        %i
                          %i
                            %i

